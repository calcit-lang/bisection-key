{
  "version": "0.0.20",
  "packageManager": "yarn@4.12.0",
  "dependencies": {
    "@calcit/procs": "^0.13.8"
  },
  "scripts": {
    "test:cr": "cr calcit.cirru --entry test",
    "test:js": "cr calcit.cirru --entry test js && node test.mjs",
    "test:wasm:compile": "cr-wasm calcit.cirru --init-fn bisection-key.wasm-probe/probe-all-count",
    "test:wasm": "yarn test:wasm:compile && node wasm-test.mjs"
  }
}
