{
  "version": "0.0.16",
  "packageManager": "yarn@4.12.0",
  "dependencies": {
    "@calcit/procs": "^0.12.25"
  },
  "scripts": {
    "test:cr": "cr --entry test -1",
    "test:js": "cr --entry test js -1 && node test.mjs",
    "test:wasm": "cr --entry wasm wasm && node wasm-test.mjs"
  }
}
