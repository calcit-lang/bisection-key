{
  "version": "0.0.16",
  "packageManager": "yarn@4.12.0",
  "dependencies": {
    "@calcit/procs": "^0.12.29"
  },
  "scripts": {
    "test:cr": "cr --entry test",
    "test:js": "cr --entry test js && node test.mjs",
    "test:wasm:compile": "cr-wasm compact.cirru --init-fn bisection-key.wasm-probe/probe-all-count",
    "test:wasm": "yarn test:wasm:compile && node wasm-test.mjs"
  }
}
