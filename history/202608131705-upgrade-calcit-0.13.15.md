# Upgrade bisection-key to Calcit 0.13.15

## Change summary

- Synced the project Calcit snapshot and `@calcit/procs` dependency to 0.13.15.
- Kept the existing canonical typed declarations and test entry unchanged.

## Knowledge point

This project already uses the current `Fn`/`String`/`Dynamic` type markers and a single `calcit.cirru` snapshot, so the upgrade is limited to the toolchain version chain and regression verification.
